```bash
$ whoami
victor hugo — backend developer

$ stack --list --primary
go · kafka · postgresql · redis

$ stack --list --infra
kubernetes · aws · terraform · docker
```
  
```bash
$ ls -la projetos/ 

NOME              DESCRIÇÃO
────────────────  ──────────────────────────────────────────────────
flowmetrics/      analytics em tempo real — 6 microsserviços, gRPC
deploy-radar/     observabilidade de deployments k8s em tempo real
notifier/         notificações assíncronas com retry e DLQ
lambdalink/       encurtador de URLs 100% serverless na AWS
go-checker/       monitoramento de uptime com jobs periódicos
```

```bash
$ contato --show
github.com/vhgomes
linkedin.com/in/vhgomes
```
